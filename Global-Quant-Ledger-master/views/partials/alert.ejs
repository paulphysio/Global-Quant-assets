<% if(success.length > 0 || error.length > 0 || warning.length > 0 ) { %> <%
if(success && success.length > 0) { %>
<div
  id="alert-3"
  class="flex px-2 py-4 my-4 mb-4 bg-green-100/30 border border-green-500"
  role="alert"
>
  <div class="ml-3 text-sm font-normal text-green-300"><%= success %></div>
</div>
<% } if (error && error.length > 0) { %>
<div
  id="alert-2"
  class="flex px-2 py-4 my-4 mb-4 bg-rose-100/30 border border-rose-500"
  role="alert"
>
  <div class="ml-3 text-sm font-normal text-rose-300"><%= error %></div>
</div>
<% } if(warning && warning.length > 0) { %>
<div
  id="alert-4"
  class="flex px-2 py-4 my-4 mb-4 bg-yellow-100/30 border border-yellow-500"
  role="alert"
>
  <div class="ml-3 text-sm font-normal text-yellow-300"><%= warning %></div>
</div>
<% } %> <% } %>
